# macOS metadata
.DS_Store
._*

# OS / IDE local
.vscode/
.idea/
Thumbs.db

# Logs / temp
*.log
*.tmp

# Backups generados localmente (los reales van como artifacts de GitHub)
backups-out/

# Service account keys — NUNCA subir al repo
*firebase-adminsdk*.json
*service-account*.json
